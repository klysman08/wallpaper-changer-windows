## 1. Design System & CSS Foundation

- [x] 1.1 Implement shadcn/ui CSS design tokens, color scales (dark and light OKLCH/HSL palettes), typography imports (`Inter Variable`, `JetBrains Mono`), and core reset in `docs/css/style.css`.
- [x] 1.2 Build reusable component styles for Buttons, Badges, Cards, Tabs, Tables, Kbd shortcuts, and Code blocks matching shadcn UI specifications.

## 2. Navigation & Theme System

- [x] 2.1 Redesign the sticky Header with app logo, clean navigation links, GitHub badge, and theme switch in `docs/index.html`.
- [x] 2.2 Implement dark/light theme toggle in `docs/js/main.js` with `localStorage` persistence and flash-prevention script in `<head>`.
- [x] 2.3 Implement mobile drawer navigation overlay and toggle interaction.

## 3. Hero & Interactive Desktop Mockup

- [x] 3.1 Construct the Hero section with release badge (`v5.1`), typography, primary & outline action buttons, and technology badges.
- [x] 3.2 Create the interactive Desktop App Window mockup mirroring `desktop/src/App.tsx` with sidebar, header, multi-monitor stage overlay, action chips, and action bar.

## 4. Features & Architecture Showcase

- [x] 4.1 Redesign the Problem vs Solution comparison matrix using shadcn table styling and clean SVG status icons.
- [x] 4.2 Redesign the Features grid into shadcn `Card` elements with Lucide-style SVG vector icons and structured descriptions.
- [x] 4.3 Overhaul the "Under the Hood" architecture section highlighting the Tauri shell and Python engine communication.

## 5. Preview Tabs, Installation & CLI Guides

- [x] 5.1 Redesign the interactive Preview tab switcher (`monitors`, `collage`, `video`, `settings`, `tray`) with realistic mockup states and descriptive panels.
- [x] 5.2 Redesign Installation guide with segmented tabs (Windows Installer vs Build from Source) and CLI commands with one-click copy buttons and feedback.

## 6. Hotkeys, Changelog & Polish

- [x] 6.1 Reformat the Global Hotkeys guide using styled `<kbd>` key combination badges.
- [x] 6.2 Modernize the Changelog release timeline with clean badges, active indicators, and version lists.
- [x] 6.3 Redesign the Call to Action section and modern Footer.
- [x] 6.4 Validate responsive layout across mobile, tablet, and desktop viewports, theme transitions, and browser accessibility.
